using UnityEngine;
using System.Collections;

using ProtoBuf;
using ProtoBuf.Meta;
using qxmobile.protobuf;

public class DebugProtoTran : MonoBehaviour {

	// Use this for initialization

	void Start () {
		/*
		EnterScene t_scene = new EnterScene();

		ChatPct t_chat = new ChatPct();

		t_chat.channel = ChatPct.Channel.guoJia;


		Troop t_troop = new Troop();

		t_troop.heroType = HeroType.TYPE_CHE;
		*/
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
