using UnityEngine;
using System.Collections;

public class DeBugSocket : MonoBehaviour {

	// Use this for initialization
	void Start () {
//        SocketTool.Instance().Connect();
//        SocketTool.Instance().SendSocketMessage(SystemInfo.deviceName);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
