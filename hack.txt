不弹出君主升级
JunZhuLevelUpManagerment.cs 
ShowInfo at end :SelefDestroy (m_HiddenObj);//tigo

主线任务自动领奖
TaskSignalInfoShow.cs 
ChargeData at 'else' end:  TaskData.Instance.GetQuestAward(m_TaskId);//tigo

新功能开启后自动点击以跳过动画
MainCityAddFunction.cs
Update  add switch case:
case AddFunctionSatatae.Click://tigo
			MYClick(m_objThis);
			break;

禁止自动弹出七日奖励
MainCityUI 搜 Res2DTemplate.Res.ONLINE_REWARD_DAY，
在case 2 的地方，注释掉该行。

自动领取星级奖励
PveStarItem.cs  在 Item_Icon.spriteName = "tint_back"; 这句后面加
				SendLingQu();

不显示进度提示框
MainCityUI.cs update 里 UIShouji.m_isPlayShouji = false;

跳过电影
EnterGame。cs  loginRet.code
if(loginRet.code==2)loginRet.code=2016;//跳过电影

自动跳过剧情对话：
UIDialogSystem.cs  update () 
if(gameObject.activeSelf)
		{
			OnPress(true);

结算界面立即显示按钮 BattleResultControllor.cs
closeAll()
layer_3.SetActive (true);